spring:
  application:
    name: buddy-administrator-service
  datasource:
    url: jdbc:mysql://localhost:3306/buddy_administrator_service
    username: root
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password: admin
server:
  address: 0.0.0.0
  port: 8082